# Contributing

Thanks for considering a contribution!

## Development

- Install Rust (stable).
- Set up git hooks: `./scripts/setup-dev.sh`

Run checks locally:

- Format: `cargo fmt --all -- --check`
- Lint: `cargo clippy -- -D warnings`
- Tests: `cargo test`

## Security

If you think you found a security issue, see `SECURITY.md`.
